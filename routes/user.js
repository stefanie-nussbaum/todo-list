import { Router } from "express";

const router = Router();

// sign up : POST
router.post('/sign-up',)
// sign in : POST
router.post('sign-in',)
// verify : GET
router.get('/verify',)
// change pwd : POST
router.post('/change-password',)

export default router;